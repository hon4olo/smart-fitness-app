export type WeightEntry = {
  id: string;
  date: string;
  weight: number;
  createdAt: string;
};

export type BodyMeasurement = {
  id: string;
  label: string;
  value: string;
  createdAt: string;
};
